import numpy as np

# Assuming 'embeddings' is your Numpy array of embeddings
embeddings = np.random.rand(10, 5)  # Example: 10 embeddings of 300 dimensions each

# Print the entire embeddings array
print(embeddings)